<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="org.zerock.mapper.MemberMapper">

<insert id="insert">
    INSERT INTO member (user_id, user_pwd, user_name, email, email_auth_key, join_date, email_verified)
    VALUES (#{user_id}, #{user_pwd}, #{user_name}, #{email}, #{email_auth_key, jdbcType=VARCHAR}, #{join_date}, #{email_verified})
</insert>
</mapper>